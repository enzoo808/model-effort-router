What is wrong with the code and what should it do instead? "Fix this code" doesn't name the failure (error, wrong output, crash?), which file/function to change, or how a correct result is verified — different assumptions about the intended behaviour would produce different fixes. Give the current vs. expected behaviour and the target file before this can be routed.
